// ECMAscript(ES)is a standard for scripting languages

"use strict"; //introduced in ECMAscript 5  
// treat all JS code in newer version
// Defines that JavaScript code should be executed in "strict mode" keep code safer and sometimes help JS engines work optimizely 
// always enabled in ES6 

// alert(3+3); error cause we are using nodejs not browser
console.log(6+9);
console.log("bruhman")

// some datatypes
let string = "something";
let number = 420;
let something = false; //boolean
let state = null;
// 1 number
// 2 bitint
// 3 string
// 4 boolean
// 5 null - standalone value(representation of empty value but not (empty value) itself)
// 6 undefined - value not defined 
// 7 symbol - unique
// these are all 7 primitive datatypes

// object (complex datatype)

console.log(typeof "this");
console.log(typeof state);
console.log(typeof null);
console.log(typeof undefined);