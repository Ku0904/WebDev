console.log(a);
var a = 9;
console.log(a);

// in undefined some memory is allocated to it [its like a placeholder like it taking up undefined value till it gets a assigned value]
// don't assign undefined to any variable cause it will take extra allocated space

// console.log(x); not defined is when no memeory is allocated to it

// (obj != undefined || obj != null):obj.something:"" to get data from object we do this
